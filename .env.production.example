# === API ===
PORT=4000
WEB_ORIGIN=https://xn--80adk3bade.xn--p1ai
NODE_ENV=production

# Python (installed inside Docker container)
PYTHON_BIN=python3
PYTHON_SCRIPT=services/python/calculate_autozakaz.py

# Storage paths (relative to project root, mapped via Docker volumes)
STORAGE_DIR=storage
SUPPLIERS_FILE=shared-data/suppliers.json
ORDER_TEMPLATE=shared-data/templates/Выгрузка заказа.xlsx

# === Auth ===
# IMPORTANT: Replace JWT_SECRET with a unique random string (64+ chars)
# Generate: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_REPLACE_WITH_RANDOM_STRING

# Initial admin credentials (used only on first startup to seed admin user)
ADMIN_LOGIN=AZ12
ADMIN_PASSWORD=As3211HH
